services:
  api1:
    image: ghcr.io/cirilord/rinha-backend-2026-api:sha-d4cbbf652fe069e37a304c7241c2d69fc316d5de
    environment:
      UNIX_SOCKET_PATH: "/shared/api1.sock"
      X_SCORE_INDEX_PATH: "/app/resources/references.idx"
    volumes:
      - shared-sockets:/shared
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.293"
          memory: "100MB"

  api2:
    image: ghcr.io/cirilord/rinha-backend-2026-api:sha-d4cbbf652fe069e37a304c7241c2d69fc316d5de
    environment:
      UNIX_SOCKET_PATH: "/shared/api2.sock"
      X_SCORE_INDEX_PATH: "/app/resources/references.idx"
    volumes:
      - shared-sockets:/shared
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.293"
          memory: "100MB"

  api3:
    image: ghcr.io/cirilord/rinha-backend-2026-api:sha-d4cbbf652fe069e37a304c7241c2d69fc316d5de
    environment:
      UNIX_SOCKET_PATH: "/shared/api3.sock"
      X_SCORE_INDEX_PATH: "/app/resources/references.idx"
    volumes:
      - shared-sockets:/shared
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.293"
          memory: "100MB"

  load-balancer:
    image: ghcr.io/cirilord/rinha-backend-2026-lb:sha-d4cbbf652fe069e37a304c7241c2d69fc316d5de
    ports:
      - "9999:9999"
    environment:
      PORT: "9999"
      WORKER_SOCKETS: "/shared/api1.sock,/shared/api2.sock,/shared/api3.sock"
    volumes:
      - shared-sockets:/shared
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    depends_on:
      - api1
      - api2
      - api3
    networks:
      - rinha
    deploy:
      resources:
        limits:
          cpus: "0.120"
          memory: "50MB"

networks:
  rinha:
    driver: bridge

volumes:
  shared-sockets:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: size=10m
